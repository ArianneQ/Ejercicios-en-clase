#include <stdio.h>

int main() {
    // Declaración del arreglo
    char vocales[] = {'a','e','i','o','u'};

    // Imprimir los elementos del arreglo
    for (int i = 0; i < 5; i++) {

        printf(" %c\n", vocales[i] );
        
    }

    return 0;
}